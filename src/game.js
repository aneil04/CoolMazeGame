export default class Game {
    //add all game variables here 
    constructor(numCellsX, numCellsY, cellSize) {
        /* 
        things to add:
        numcellsx
        numcellxy
        cellsize
        cells
        */
        this.numCellsX = numCellsX;
        this.numCellsY = numCellsY;
        this.cellSize = cellSize;
    }
}